/**
 * @fileoverview Utility functions for parsing user input and more.
 */

/**
 * @description Turns the JSON of the user's reponses into a string so the embedding model can understand it better
 * @param {JSON} userInput The user's responses the questionnaire
 * @returns {string} A string version of userInput
 */

async function parseUserInput(userInput) {
    let parsedUserInput;

    return parsedUserInput;
}
